{
  "name": "itsuki-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server with Streamable HTTP Transport",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node index.ts",
    "docker:build": "docker build -t itsuki-mcp-server .",
    "docker:run": "docker run -p 3000:3000 itsuki-mcp-server"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

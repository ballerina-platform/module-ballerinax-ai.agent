# Agent with Multi actions

1) Set `openAPIToken` and `gmailToken` configs in `Config.toml` (Keep rest of the configs as it is)
2) Run `bal run -- {wifiOwnerEmail} {wifiUsername} {wifiPassword} {recipientEmail}`